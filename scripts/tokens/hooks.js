/**
 * Tokens feature hooks.
 * Manages hexagonal token shapes for hex grids.
 *
 * TODO Phase 3: Extract hex token hooks from core/init.js and implement here:
 *
 * import { applyHexTokenSetting } from './hex-tokens.js'
 *
 * Hooks.on('drawToken', (token, context) => {
 *     // Apply hex mask to token canvas
 * })
 *
 * Hooks.on('refreshToken', (token) => {
 *     // Refresh hex token display
 * })
 *
 * Hooks.on('updateSetting', (setting, value) => {
 *     if (setting.key === 'Ilaris.hexTokens') applyHexTokenSetting()
 * })
 *
 * export function tokensReady() {
 *     applyHexTokenSetting()
 * }
 */
