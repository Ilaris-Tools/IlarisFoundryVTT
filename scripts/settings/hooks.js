/**
 * Settings feature hooks.
 * Handles scene config and settings config UI extensions.
 *
 * TODO Phase 3: Move renderSettingsConfig and renderSceneConfig hooks here from core/init.js.
 *
 * import { registerIlarisGameSettings } from './configure-game-settings.js'
 *
 * Hooks.on('renderSettingsConfig', (app, html, data) => {
 *     // Add Ilaris-specific settings UI (currently in core/init.js)
 * })
 *
 * Hooks.on('renderSceneConfig', (app, html, data) => {
 *     // Add scene environment fields (currently in core/init.js)
 * })
 */
