<section class="tab notizen {{tab.cssClass}}" data-group="primary" data-tab="notizen">
    {{#if editable}}
    <prose-mirror
        name="system.notes"
        button="true"
        editable="{{editable}}"
        toggled="false"
        value="{{system.notes}}">
        {{{notesHTML}}}
    </prose-mirror>
    {{else}}
    {{{notesHTML}}}
    {{/if}}
</section>