/**
 * Dice feature hooks.
 *
 * TODO Phase 3: Move renderChatMessageHTML hook here from core/init.js.
 *
 * import { formatDiceFormula } from '../../core/utilities.js'
 * import { IlarisChatMessage } from './chatutilities.js'
 *
 * Hooks.on('renderChatMessageHTML', (message, html, data) => {
 *     // Format dice formula display in chat
 *     // (currently handled in core/init.js with TODO Phase 2.4 comment)
 * })
 */
