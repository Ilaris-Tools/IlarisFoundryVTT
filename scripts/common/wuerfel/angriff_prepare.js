export function angriffUpdate(html, actor, item) {
    /* schreibt alle inputs aus dem formular in das item
    TODO: könnte das nicht direkt via template passieren für einen großteil der werte?
    */
    // async function nahkampfUpdate(html, actor, item) {
    // let itemId = item.id;
    // item = actor.items.get(itemId);
}
