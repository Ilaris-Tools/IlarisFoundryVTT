---
description: Sync delta specs from a change to main specs
---

Read root [`AGENTS.md`](../../AGENTS.md), then read the **Sync (`openspec-sync-specs`)** section of [`OPENSPEC_OPERATIONS.md`](../../.agents/OPENSPEC_OPERATIONS.md) completely and follow it exactly. This command file intentionally contains only Claude command metadata and the pointer to the canonical operation body.
