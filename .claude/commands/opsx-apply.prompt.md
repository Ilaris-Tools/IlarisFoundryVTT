---
description: Implement tasks from an OpenSpec change (Experimental)
---

Read root [`AGENTS.md`](../../AGENTS.md), then read the **Apply (`openspec-apply-change`)** section of [`OPENSPEC_OPERATIONS.md`](../../.agents/OPENSPEC_OPERATIONS.md) completely and follow it exactly. This command file intentionally contains only Claude command metadata and the pointer to the canonical operation body.
