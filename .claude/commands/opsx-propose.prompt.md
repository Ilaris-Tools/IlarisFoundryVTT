---
description: Propose a new change - create it and generate all artifacts in one step
---

Read root [`AGENTS.md`](../../AGENTS.md), then read the **Propose (`openspec-propose`)** section of [`OPENSPEC_OPERATIONS.md`](../../.agents/OPENSPEC_OPERATIONS.md) completely and follow it exactly. This command file intentionally contains only Claude command metadata and the pointer to the canonical operation body.
