{{log 'uebernatuerliche' this}}
<section class="{{cssClass}}">
    <div>
        <div>
            <header class="sheet-header">
                <div class="image-edit-container">
                    <img class="profile-img" src="{{item.img}}" data-action="editImage" title="{{item.name}}" />
                    <i class="fas fa-edit image-edit-overlay"></i>
                </div>
                <div class="header-fields">
                    <h1 class="charname">
                        <input name="name" type="text" value="{{item.name}}" />
                    </h1>
                </div>
            </header>
        </div>
        <div>
            <div class="flexrow">
                <h6>Attribute</h6>
                <h6></h6>
                <h6></h6>
                <h6>Basis</h6>
                {{#if hasOwner}}
                <h6>FW</h6>
                {{/if}}
                <!-- !!!!Wichtig!!!!: PW geht so nicht: Er übernimmt die Berechnung nicht. Das ist auch mein Problem beim array bei den Talenten, denke ich -->
            </div>
            <div class="flexrow">
                <select name="system.attribut_0">
                    {{selectOptions config.attribute selected=item.system.attribut_0 valueAttr="value" labelAttr="label"}}
                </select>
                <select name="system.attribut_1">
                    {{selectOptions config.attribute selected=item.system.attribut_1 valueAttr="value" labelAttr="label"}}
                </select>
                <select name="system.attribut_2">
                    {{selectOptions config.attribute selected=item.system.attribut_2 valueAttr="value" labelAttr="label"}}
                </select>
                {{#if hasOwner}}
                <input
                    name="system.fw"
                    value="{{item.system.fw}}"
                    type="text"
                    data-dtype="Number"
                />
                {{/if}}
            </div>
            <label>Beschreibung</label>
            <textarea rows="6" name="system.text">{{item.system.text}}</textarea>
        </div>
    </div>
</section>
