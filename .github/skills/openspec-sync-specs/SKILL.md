---
name: openspec-sync-specs
description: Sync delta specs from a change to main specs. Use when the user wants to update main specs with changes from a delta spec, without archiving the change.
license: MIT
compatibility: Requires openspec CLI.
metadata:
    author: openspec
    version: '1.0'
    generatedBy: '1.5.0'
---

Read root [`AGENTS.md`](../../../AGENTS.md), then read the **Sync (`openspec-sync-specs`)** section of [`OPENSPEC_OPERATIONS.md`](../../../.agents/OPENSPEC_OPERATIONS.md) completely and follow it exactly. This file intentionally contains only GitHub skill metadata and the pointer to the canonical operation body.
